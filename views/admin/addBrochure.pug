extends ./header.pug
block append head 
  link(href='/css/addProduct.css', rel='stylesheet')
block content
    form(action = "/admin/addBrochure", enctype="multipart/form-data" method = "POST" id="product" style="padding:10px; margin:5px; text-align:center;")
      label(for ="brochures") Upload Brochures:
      input(type = "file" name="brochures" required multiple) 
      br
      br
      button(class="btn btn-primary" type = "submit") Add brochures